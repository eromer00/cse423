dog(int x, y[])
{
    output(x);
    output(y[3]);
    outnl();
}

cat(int x, y[])
{
    output(x);
    output(y[3]);
    outnl();
    dog(x, y);
}

main() {
    int x, y[10];

    x = 666;
    y[3] = 777;

    output(x);
    output(y[3]);
    outnl();

    cat(x, y);
}
